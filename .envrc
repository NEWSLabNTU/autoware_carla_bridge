#!/usr/bin/env bash
# direnv configuration for autoware_carla_bridge

# Source Autoware 1.5.0 Debian (includes ROS 2 Humble)
if [ -f /opt/autoware/1.5.0/setup.bash ]; then
    source /opt/autoware/1.5.0/setup.bash
else
    # Fallback: source ROS 2 Humble directly
    source /opt/ros/humble/setup.bash
fi

# Source local colcon install if it exists
if [ -f install/setup.bash ]; then
    watch_file install/setup.bash
    source install/setup.bash
fi

export DISPLAY=:1
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
